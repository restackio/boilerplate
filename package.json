{
  "name": "boilerplate-app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "start": "turbo start",
    "lint": "turbo lint",
    "lint:fix": "turbo lint --fix",
    "type-check": "tsc --noEmit",
    "check": "npm run type-check && npm run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "rm -rf node_modules && rm -rf apps/*/node_modules && rm -rf packages/*/node_modules",
    "infra:start": "docker-compose -f docker-compose.dev.yml up -d",
    "infra:stop": "docker-compose -f docker-compose.dev.yml down",
    "infra:restart": "docker-compose -f docker-compose.dev.yml restart",
    "infra:logs": "docker-compose -f docker-compose.dev.yml logs -f",
    "infra:reset": "docker-compose -f docker-compose.dev.yml down -v && docker-compose -f docker-compose.dev.yml up -d",
    "infra:ps": "docker-compose -f docker-compose.dev.yml ps",
    "db:connect": "docker-compose -f docker-compose.dev.yml exec postgres psql -U postgres -d boilerplate_db",
    "db:reset": "docker-compose -f docker-compose.dev.yml exec postgres psql -U postgres -d boilerplate_db -c 'DROP SCHEMA public CASCADE; CREATE SCHEMA public;' && docker-compose -f docker-compose.dev.yml exec postgres psql -U postgres -d boilerplate_db -f /docker-entrypoint-initdb.d/01-schema.sql",
    "db:seed:workspace": "docker-compose -f docker-compose.dev.yml exec postgres psql -U postgres -d boilerplate_db -f /docker-entrypoint-initdb.d/02-workspace-seed.sql",
    "db:seed:agents": "docker-compose -f docker-compose.dev.yml exec postgres psql -U postgres -d boilerplate_db -f /docker-entrypoint-initdb.d/03-agents-seed.sql",
    "db:seed": "pnpm db:seed:workspace && pnpm db:seed:agents",
    "db:setup": "pnpm db:reset && pnpm db:seed",
    "db:clean": "docker-compose -f docker-compose.dev.yml exec postgres psql -U postgres -d boilerplate_db -c 'TRUNCATE TABLE agent_runs, tasks, agents, users, workspaces RESTART IDENTITY CASCADE;'",
    "docker:build": "docker-compose -f docker-compose.yml build",
    "docker:up": "docker-compose -f docker-compose.yml up -d",
    "docker:down": "docker-compose -f docker-compose.yml down",
    "docker:logs": "docker-compose -f docker-compose.yml logs -f",
    "docker:restart": "docker-compose -f docker-compose.yml restart",
    "docker:reset": "docker-compose -f docker-compose.yml down -v && docker-compose -f docker-compose.yml up -d --build",
    "docker:ps": "docker-compose -f docker-compose.yml ps"
  },
  "devDependencies": {
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "prettier": "^3.5.3",
    "turbo": "^2.5.4",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=20"
  }
}
