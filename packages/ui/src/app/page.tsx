// This is a minimal page file to satisfy Next.js lint requirements
// This UI package is a component library, not a standalone app
export default function Page() {
  return <div>UI Component Library</div>
}
