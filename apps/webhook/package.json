{
  "name": "@boilerplate/webhook",
  "version": "0.0.1",
  "description": "Dedicated webhook server for creating tasks from external events",
  "private": true,
  "scripts": {
    "dev": "uv run dev",
    "start": "uv run start",
    "build": "uv sync --frozen",
    "lint": "uv run ruff check src",
    "lint:fix": "uv run ruff check --fix src",
    "format": "uv run ruff format src",
    "type-check": "echo 'Python webhook does not need type checking'",
    "postinstall": "mkdir -p node_modules"
  },
  "dependencies": {},
  "devDependencies": {}
}
