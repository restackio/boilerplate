{
  "name": "@boilerplate/backend-py",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uv sync && uv run dev",
    "start": "uv run start",
    "schedule": "uv run schedule",
    "install": "uv sync",
    "build": "echo 'Python backend does not need build step'",
    "lint": "ruff check .",
    "lint:fix": "ruff check . --fix",
    "type-check": "echo 'Python backend does not need type checking'"
  },
  "dependencies": {},
  "devDependencies": {}
} 